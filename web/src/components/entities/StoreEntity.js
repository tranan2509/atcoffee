class StoreEntity {

  constructor(name, address, timeOpen, timeClose) {
    this.name = name;
    this.address = address;
    this.timeOpen = timeOpen;
    this.timeClose = timeClose;
  }
}

export default StoreEntity;